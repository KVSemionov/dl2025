# Homework: Semi-Supervised Learning

## Задача

Данная задача моделирует проблему semi-supervised learning, где доступны как размеченные, так и неразмеченные данные.

## Что сделали

1. Запустите baseline (`baseline/train_baseline.ipynb`) и убедились, что все работает корректно.

2. Реализовали решение через pseudo_label, которое улучшит метрику **F1 macro** на тестовой выборке.

3. **Основная цель**: достигли **F1 macro ≥ 0.32** (33%) на тестовой выборке.

## Данные

- `train_labeled.csv` - размеченные данные для обучения
- `train_unlabeled.csv` - неразмеченные данные
- `test.csv` - тестовая выборка

## Baseline

Baseline использует только размеченные данные и достигает F1 macro ~0.27 на тестовой выборке.

## PseudoLabelLine

Реализовывает псевдо-метки и улучшает F1 macro до 33%.
